# Local environment variables for docker-compose (copy to `.env` and edit)
# These values are safe defaults for local development only. DO NOT use these
# credentials in production.

DB_ROOT_PASSWORD=root_local_password
DB_NAME=wp_local
DB_USER=wp_user
DB_PASSWORD=wp_pass

# Port exposed on the host for WordPress. Example: http://localhost:8000
WORDPRESS_PORT=8000
